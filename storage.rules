rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Backend uploads use the Admin SDK. Client-side storage access is denied.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
