rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All business-data access goes through the trusted NestJS backend.
    // Firebase Admin SDK requests bypass these client rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
